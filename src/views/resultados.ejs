<%- include('../views/layouts/main_cliente.ejs'); %>
<div>
  <div class="contendorcateg">
    <h1 class="tituloGrande">Resultados de búsqueda</h1>
  </div>
    <div id="resultado">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th></th>
                  <th></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% trabajo.forEach(function(user){ %>
                <tr class="table-primary">
                  <td>
                    <div class="iconoss">
                      <%= user.nombre %>
                    </div>
                  </td>
                  <td>
                    <div>
                      <%= user.apellido %>
                      <p>Aqui va la descripcion</p>
                    </div>
                  </td>
                  <td>
                    <div class="comentarioalig">
                      <a href="/calificar">
                        <input type="button" class="btn btn-info solicitar" value="Solicitar">
                      </a>
                    </div>
                  </td>
                </tr>
                <% }); %>
              </tbody>
            </table>
    </div>

  <div class="comentarioalig">
    <a href="/categorias">
      <input type="button" class="btn btn-primary" name="regresar" value="Regresar">
    </a>
  </div>
</div>

<script>
  const solicitarBtn = document.querySelectorAll(".solicitar");

  solicitarBtn.forEach(function(btn) {
    btn.addEventListener("click", function() {
      const trabajador = this.parentNode.parentNode.parentNode;
      const nombre = trabajador.querySelector(".iconoss").textContent;
      const apellido = trabajador.querySelector("td div").firstChild.nextSibling.textContent;
      console.log("9999999999999999")
      console.log(nombre, apellido);
    });
  });
</script>
